# Streamlit Secrets Template
# Copy this to Streamlit Cloud: Manage app → Secrets
# Replace all placeholder values with your actual values

# AWS Credentials
# Get these from AWS IAM Console → Users → Security Credentials
AWS_ACCESS_KEY_ID = "AKIAIOSFODNN7EXAMPLE"
AWS_SECRET_ACCESS_KEY = "wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY"

# AWS Region
# Use the region where your AgentCore runtime is deployed
AWS_DEFAULT_REGION = "us-west-2"

# Agent Runtime ARN
# Get this by running: aws ssm get-parameter --name /app/returnsrefunds/agentcore/runtime_arn --query Parameter.Value --output text
# Format: arn:aws:bedrock-agentcore:REGION:ACCOUNT_ID:runtime/RUNTIME_ID
RUNTIME_ARN = "arn:aws:bedrock-agentcore:us-west-2:123456789012:runtime/abc123def456"

# Optional: If you need other SSM parameters, add them here with the last part of the path as the key
# Example: /app/myapp/database/url -> DATABASE_URL = "value"
